# Servidor MySQL no Linux
Instalação e configuração do servidor MySQL no Ubuntu.

## Instalação
Comando para instalar:
```bash
sudo apt install mysql-server
```

## Configuração
Para acessar o banco de dados como `root`:
```bash
mysql -u root -p
```
